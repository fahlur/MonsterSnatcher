name: MonsterSnatcher
main: me.Fahlur.MonsterSnatcher.main
version: 1.0
description: Mob Capture Plugin
commands:
   MonsterSnatcher:
    usage: /<command>
    aliases: [ms]
    description: Main command returns version
permissions:
   ms.reload:
    description: Ability to use monsterSnatchers reload
   ms.catchable:
    description: Ability to use capture mobs
